# Скопируйте в .env и заполните: copy .env.example .env  (файл .env в git не попадает)

ADDR=127.0.0.1:8087
DATA_DIR=./runtime
ADMIN_TOKEN=
# Для локальной админки на другом порту, например http://127.0.0.1:5174 — иначе можно оставить пустым
CORS_ALLOWED_ORIGIN=
